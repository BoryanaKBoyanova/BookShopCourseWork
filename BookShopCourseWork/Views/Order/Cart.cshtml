@{
    ViewData["Title"] = "Shopping cart";
}

<div class="container">
    <div class="row">
        <table class="table table-striped table-primary">
            <thead>
                <tr>
                    <th scope="col">Title book</th>
                    <th scope="col">Quantity</th>
                    <th scope="col">Price</th>
                    <th scope="col" style="max-width: 30px;"></th>
                </tr>
            </thead>
            <tbody id="cart">
            </tbody>

        </table>
        <p id="emptyCart" style="display: none;" class="text-center text-primary fs-3">Your shopping cart is empty!</p>
    </div>
    <div class="row">
        <div class="col">
            <p class="fw-bold fs-4">Total: € <span id="totalPrice"></span></p>
            <button class="btn btn-primary" onclick="updateShoppingCart()">Update shopping cart</button>
        </div>
        <div class="col">
            <a class="btn btn-primary float-end" id="finishOrderButton" href="./FinishOrder">Finish order</a>
        </div>
    </div>
</div>
<script>getAllProductsInCart();
</script>